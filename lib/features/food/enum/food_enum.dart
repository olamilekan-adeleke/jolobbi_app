enum FoodVendorStatus {
  unknown,
  getPopularBusy,
  getPopularError,
  getPopularSuccess,
  getAllBusy,
  getAllError,
  getAllSuccess,
}

enum FoodItemStatus { unknown, busy, moreBusy, error, moreError, success }
